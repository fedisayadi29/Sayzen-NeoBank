# Server
PORT=5000

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d

# Database (SQLite - no configuration needed, file created automatically)
# DB_PATH=./sayzen.db  # Optional: custom path
